require(['order!libs/underscore', 'order!libs/backbone', 'libs/bootstrap', 'libs/handlebars-1.0.0.beta.6'], 
  function() {
    console.debug('Application initiated.');

    // Ready to start!
    
  
  }
);
